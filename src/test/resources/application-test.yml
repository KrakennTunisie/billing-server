spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  config:
    import: ""

  storage:
    local:
      root-path: ./target/test-storage
      public-base-url: http://billing-service:8081 #Docker :billing-service